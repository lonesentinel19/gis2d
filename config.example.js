var mapLoc = "img/mymap.png";
var scale = .8382; // 1 px = scale(cm) (e.g: a value of .8382 means that one pixel on the screen equals 1 cm)
var maxZoom = 2.0;
var minZoom = 0.5;
var height = 492; // in pixels, original size
var width = 992; // in pixels, original size
var locations = {
	'park': [0, 44, 229, 344, "A cool park with trees."]
	}
